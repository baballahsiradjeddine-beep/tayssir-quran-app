// Firebase Cloud Messaging service worker
// Push notifications are disabled on web - this file is intentionally empty.
// Removing importScripts prevents Safari's ITP privacy warning and Chrome loading issues.

self.addEventListener('push', function() {});
self.addEventListener('notificationclick', function() {});
